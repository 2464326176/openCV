# 早期环境搭建草稿（归档）

> **归档说明**：本文件由仓库根目录的 `Untitled-1.txt` 归档而来，是项目**最初版本**的
> 手写草稿，记录了从零搭建 Windows + MinGW + OpenCV 环境的步骤与图像处理基础速记。
>
> **它的内容已被以下文档接管并大幅扩充**，日常请优先看这些：
>
> | 草稿章节 | 现归属 |
> |---------|--------|
> | 一、安装（MinGW / CMake / OpenCV 源码） | [README.md §1 环境与安装](../README.md#1-环境与安装windows--mingw64--cmake--opencv-源码编译) |
> | 二、图像处理 · 图像滤波 | [README.md §5.1 图像滤波](../README.md#51-图像滤波image-filtering) + [docs/principles.md](principles.md) |
> | 二、图像处理 · 图像变换（Canny/Sobel/霍夫/重映射/仿射/直方图） | [README.md §5.2 图像变换](../README.md#52-图像变换image-transform) + [docs/ch02_imgproc.md](ch02_imgproc.md) |
> | 各算法的可运行实现 | [learn/](../learn)、[algorithms/](../algorithms) |
>
> 保留本文件的唯一理由：**它记录了当初的环境选型决策**（为什么选 sjlj 而不是 seh），
> 这类"为什么"在正式文档里往往被精简掉。

---

以下是原始草稿内容（仅做换行与标题层级的轻度整理，未改动原意与原文措辞）。

***

# 计算机视觉

## 一、安装

环境：OpenCV + MinGW64 + CMake

在 Ubuntu 上学习一段时间感觉挺不方便的，还是 Windows 比较方便操作点，
整理了下 Windows 的步骤，给大家分享下。

### 1.1 MinGW64 下载

地址：<https://sourceforge.net/projects/mingw-w64/files/>

MinGW-W64 GCC-8.1.0 下有这些变体：

```
x86_64-posix-sjlj
x86_64-posix-seh
x86_64-win32-sjlj
x86_64-win32-seh
i686-posix-sjlj
i686-posix-dwarf
i686-win32-sjlj
i686-win32-dwarf
```

可以看到有很多版本，这里要选择 **`x86_64-posix-sjlj`**。

- **POSIX** 表示可移植操作系统接口（Portable Operating System Interface of UNIX，缩写 POSIX）。
- **seh** 是新发明的，而 **sjlj** 则是古老的。seh 性能比较好，但不支持 32 位；
  sjlj 稳定性好，支持 32 位，兼容前面的平台。

>  **这个选型被本项目沿用了下来**——`mingw-build/` 里的预编译产物就是用
> sjlj 工具链编的。如果你要自己重编 OpenCV，务必选同一个变体，否则链接期会出
> 莫名其妙的符号冲突。详见 [README.md §1.1](../README.md#11-mingw-w64-下载)。

### 1.2 CMake

<https://cmake.org/>

### 1.3 OpenCV 源码

- 源码：<https://github.com/opencv/opencv>
- 详细的安装步骤：<https://blog.huihut.com/2018/07/31/CompiledOpenCVWithMinGW64/>

---

## 二、图像处理（imgproc — Image Processing）

### 图像滤波

**目的**：抽出对象的特征作为图像识别的特征模式；消除图像数字化所混入的噪音。

常见滤波器：

- 方框滤波
- 均值滤波
- 高斯滤波
- 中值滤波
- 双边滤波

**平滑处理**分两大类：

- **线性滤波**：中值滤波、双边滤波
- **形态学滤波**：腐蚀、膨胀、开运算、闭运算、形态学梯度、顶帽、黑帽

其他常用操作：

- 漫水填充
- 图像尺寸缩放
- 阈值化

### 图像变换

**边缘检测**：Canny 算子、Sobel 算子、Laplacian 算子、Scharr 滤波器。

**边缘检测通用三步走：滤波 → 增强 → 检测。**

#### Canny 算子步骤

1. **【第一步】滤波**

   边缘检测的算法主要是基于图像强度的一阶和二阶导数，但导数通常对噪声很敏感，
   因此必须采用滤波器来改善与噪声有关的边缘检测器的性能。常见的滤波方法主要有
   高斯滤波，即采用离散化的高斯函数产生一组归一化的高斯核，然后基于高斯核函数
   对图像灰度矩阵的每一点进行加权求和。

2. **【第二步】增强**

   增强边缘的基础是确定图像各点邻域强度的变化值。增强算法可以将图像灰度点邻域
   强度值有显著变化的点凸显出来。在具体编程实现时，可通过计算梯度幅值来确定。

3. **【第三步】检测**

   经过增强的图像，往往邻域中有很多点的梯度值比较大，而在特定的应用中，这些点
   并不是要找的边缘点，所以应该采用某种方法来对这些点进行取舍。实际工程中，
   常用的方法是通过阈值化方法来检测。

> 另外，需要注意：下文中讲到的 Laplacian 算子、Sobel 算子和 Scharr 算子都是
> 带方向的，所以示例中我们分别写了 X 方向、Y 方向和最终合成的效果图。

#### Sobel 算子步骤

1. **Sobel 算子的基本概念**

   Sobel 算子是一个主要用于边缘检测的离散微分算子（discrete differentiation operator）。
   它结合了高斯平滑和微分求导，用来计算图像灰度函数的近似梯度。在图像的任何一点
   使用此算子，都将会产生对应的梯度矢量或是其法矢量。

2. **Sobel 算子的计算过程**

   我们假设被作用图像为 `I`，然后进行如下操作：

   **(1) 分别在 x 和 y 两个方向求导。**

   水平变化：将 `I` 与一个奇数大小的内核 `Gx` 进行卷积。当内核大小为 3 时：

   ```
   Gx = [ -1  0  +1 ]        Gy = [ -1  -2  -1 ]
        [ -2  0  +2 ]             [  0   0   0 ]
        [ -1  0  +1 ]             [ +1  +2  +1 ]
   ```

   垂直变化：将 `I` 与一个奇数大小的内核 `Gy` 进行卷积。当内核大小为 3 时，
   计算结果如上图右侧。

   **(2) 在图像的每一点，结合以上两个结果求出近似梯度：**

   ```
   G = √(Gx² + Gy²)
   ```

   另外有时，也可用下面更简单的公式代替：

   ```
   G = |Gx| + |Gy|
   ```

#### 其他变换

- 霍夫变换
- 重映射
- 仿射变换
- 直方图均衡化
